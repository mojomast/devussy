# Project Handoff Report

**Generated:** {{ timestamp }}  
**Project:** {{ handoff.project_name }}  
**Handoff Phase:** {{ handoff.current_phase | default("N/A") }}  
**Status:** {{ handoff.project_status | default("In Progress") }}

---

## Project Summary

{{ handoff.project_summary }}

**Key Objectives:**
{% for objective in handoff.project_objectives %}
- {{ objective }}
{% endfor %}

## Current Progress

### ✅ Completed Phases
{% for phase in handoff.completed_phases %}
- **Phase {{ phase.number }}**: {{ phase.name }}
  - {{ phase.completion_date | default("Recently completed") }}
  - {{ phase.summary }}
{% endfor %}

### 🔄 Current Phase
{% if handoff.current_phase_details %}
**Phase {{ handoff.current_phase_details.number }}**: {{ handoff.current_phase_details.name }}

{{ handoff.current_phase_details.description }}

**Progress:** {{ handoff.current_phase_details.completed_steps | default(0) }}/{{ handoff.current_phase_details.total_steps }} steps completed

#### Completed Steps
{% for step in handoff.current_phase_details.completed_steps_list %}
- ✅ {{ step.number }}: {{ step.title }}
{% endfor %}

#### Next Steps
{% for step in handoff.next_steps %}
{{ step.number }}. **{{ step.title }}**
   - {{ step.description }}
   {% if step.priority %}
   - *Priority:* {{ step.priority }}
   {% endif %}
   {% if step.estimated_time %}
   - *Estimated Time:* {{ step.estimated_time }}
   {% endif %}
{% endfor %}

{% endif %}

## Implementation Guidelines

### Code Development Rules
{% for rule in handoff.development_rules %}
- {{ rule }}
{% endfor %}

### Git Workflow
{% for rule in handoff.git_rules %}
- {{ rule }}
{% endfor %}

### Quality Standards
{% for standard in handoff.quality_standards %}
- {{ standard }}
{% endfor %}

## Technical Context

### Architecture Overview
{{ handoff.architecture_summary }}

### Key Components
| Component | Status | Location | Notes |
|-----------|--------|----------|-------|
{% for component in handoff.key_components %}
| {{ component.name }} | {{ component.status }} | {{ component.location }} | {{ component.notes | default("-") }} |
{% endfor %}

### Dependencies
- **Core Dependencies:** {{ handoff.core_dependencies | join(", ") }}
- **Dev Dependencies:** {{ handoff.dev_dependencies | join(", ") }}

## File Structure

```
{{ handoff.project_structure }}
```

## Configuration

### Environment Variables
{% for env_var in handoff.environment_variables %}
- `{{ env_var.name }}`: {{ env_var.description }}
{% endfor %}

### Config Files
{% for config in handoff.config_files %}
- `{{ config.path }}`: {{ config.description }}
{% endfor %}

## Testing Strategy

{{ handoff.testing_strategy }}

**Test Coverage:** {{ handoff.test_coverage | default("TBD") }}

## Known Issues

{% for issue in handoff.known_issues %}
- **{{ issue.severity }}**: {{ issue.description }}
  - *Status:* {{ issue.status }}
  - *Workaround:* {{ issue.workaround | default("None") }}
{% endfor %}

## Success Criteria

{% for criterion in handoff.success_criteria %}
- [ ] {{ criterion }}
{% endfor %}

## Getting Started (Quick Start)

1. **Environment Setup**
   ```bash
   {{ handoff.setup_commands | join('\n   ') }}
   ```

2. **Run Tests**
   ```bash
   {{ handoff.test_commands | join('\n   ') }}
   ```

3. **Next Immediate Tasks**
   {% for task in handoff.immediate_tasks %}
   - {{ task }}
   {% endfor %}

## Resources and Documentation

{% for resource in handoff.resources %}
- **{{ resource.name }}**: [{{ resource.url }}]({{ resource.url }}) - {{ resource.description }}
{% endfor %}

---

## Handoff Notes

{{ handoff.handoff_notes }}

**When to Generate Next Handoff:**
{% for trigger in handoff.next_handoff_triggers %}
- {{ trigger }}
{% endfor %}

---

*Generated by DevPlan Orchestrator*【cursor†source】